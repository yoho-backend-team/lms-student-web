/* eslint-disable @typescript-eslint/no-explicit-any */
export const selectcertificate = (state: any) => state.certificateSlice.data;
export const selectLoading = (state: any) => state.certificateSlice.loading;
